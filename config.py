# MySQL connection settings
# Change these to match your own MySQL setup before running the app.

DB_CONFIG = {
    "host": "localhost",
    "user": "root",
    "password": "Admin@1",
    "database": "bank_db"
}
